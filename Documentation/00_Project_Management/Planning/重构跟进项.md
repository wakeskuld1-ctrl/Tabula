# 重构跟进项

## 阶段一

### 任务一：建立最小分层骨架并迁移 health 接口
- 状态：完成
- 负责人：AI
- 开始时间：2026-02-08
- 目标：分层骨架 + health 迁移
- 风险：模块路径与路由绑定错误
- 验收：health 响应一致，main.rs 无业务逻辑

### 任务二：迁移 grid 查询链路
- 状态：完成
- 子任务 2.1：抽取 get_grid_data 接口编排到 api 层（main.rs, api/grid_handler.rs, api/mod.rs）
- 子任务 2.1 状态：完成
- 子任务 2.1 关键点：get_grid_data 下沉至 api/grid_handler.rs，main.rs 仅保留路由绑定，通过 api/mod.rs 统一出口
- 子任务 2.2：抽取查询与分页逻辑到 services 层（main.rs, services/grid_service.rs, services/mod.rs）
- 子任务 2.2 状态：完成
- 子任务 2.2 关键点：查询与分页逻辑下沉至 services/grid_service.rs，api 层仅保留参数与响应编排
- 子任务 2.3：抽取会话注册与元数据获取到 services 层（main.rs, services/grid_service.rs, services/mod.rs）
- 子任务 2.3 状态：完成
- 子任务 2.3 关键点：会话切换、上下文注册与元数据获取统一下沉至 services/grid_service.rs

### 任务三：迁移 execute_sql 链路
- 状态：完成
- 关键点：execute_sql/get_plan 逻辑下沉至 services，api 层仅负责参数与响应

### 任务四：迁移 upload 逻辑
- 状态：完成
- 子任务 4.1：抽取 upload 入口到 api 层（main.rs, api/upload_handler.rs, api/mod.rs）
- 子任务 4.1 状态：完成
- 子任务 4.1 关键点：upload api 仅保留参数与响应编排
- 子任务 4.2：下沉 upload 业务逻辑到 services 层（api/upload_handler.rs, services/upload_service.rs, services/mod.rs）
- 子任务 4.2 状态：完成
- 子任务 4.2 关键点：文件写入、转码回退、注册与 metadata 持久化集中在服务层
- 子任务 4.3：迁移 register_table 链路到 api-service（main.rs, api/register_handler.rs, services/register_service.rs, services/mod.rs）
- 子任务 4.3 状态：完成
- 子任务 4.3 关键点：表名清洗与注册逻辑下沉到服务层，api 仅编排

## 阶段二

### 任务一：缓存体系解耦
- 状态：进行中
- 开始时间：2026-02-09
- 子任务 1.1：梳理 CacheManager 依赖面与调用入口
- 子任务 1.1 状态：进行中
- 子任务 1.1 关键点：定位所有静态入口与跨层依赖
- 子任务 1.1 调用入口：main.rs、services/register_service.rs、services/upload_service.rs、datasources/sqlite.rs、datasources/oracle.rs、cache_e2e_test.rs、cache_stress_test.rs
- 子任务 1.1 静态依赖：VOLATILITY_TRACKER、INFLIGHT_REGISTRY、METRICS_REGISTRY、L2_CACHE、L1_CACHE_INDEX、GLOBAL_MEMORY_USAGE、IS_EVICTING、TEST_MEMORY_LIMIT、TEST_DISK_USAGE、BYPASS_SEMAPHORE
- 子任务 1.1 关键职责：缓存键生成、波动性判定、L1/L2 读写、单飞行合并、L2/TTI 驱逐、L1 磁盘压力驱逐、影子 Parquet 生成
- 子任务 1.1 外部依赖：文件系统与路径、PowerShell 系统信息、tokio 任务调度、datafusion/arrow/parquet、ExcelDataSource
- 子任务 1.2：抽离 CacheManager 全局静态依赖
- 子任务 1.2 状态：未开始
- 子任务 1.2 关键点：静态访问点替换为依赖注入入口
- 子任务 1.3：提供可注入的缓存接口与实现
- 子任务 1.3 状态：未开始
- 子任务 1.3 关键点：接口边界清晰，默认实现可替换
- 里程碑 1：完成依赖梳理与改造清单
- 里程碑 2：完成静态依赖替换与可注入接口
- 关键点：缓存依赖可注入、可替换

### 任务二：数据源扩展机制
- 状态：未开始
- 子任务 2.1：统一数据源注册入口
- 子任务 2.2：规范数据源生命周期与配置
- 关键点：新增数据源不修改主流程

### 任务三：注册与元数据流程梳理
- 状态：未开始
- 子任务 3.1：统一注册流程与表名规范
- 子任务 3.2：梳理 metadata 写入与读取一致性
- 关键点：注册与元数据保持一致性

### 任务四：回归与性能验证
- 状态：未开始
- 子任务 4.1：回归用例覆盖关键链路
- 子任务 4.2：建立性能基线并对比
- 关键点：回归用例通过且性能不退化
